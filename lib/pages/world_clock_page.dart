import 'package:flutter/material.dart';

class WorldClockPage extends StatelessWidget{
  WorldClockPage({super.key});

  @override
  Widget build(BuildContext context) {
      return Stack(
        children: [
          Container(color: Color(0xff161618)),
        ],
      );
  }
}