import 'package:flutter/material.dart';

class BedtimePage extends StatelessWidget{
  BedtimePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [
        Container(color: Color(0xff161618)),
      ],
    );
  }
}